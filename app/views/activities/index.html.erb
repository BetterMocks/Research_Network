<h1 align="right">notifications</h1>

<% @activities.each do |activity| %>
  <div class="activity">
    <%= link_to activity.owner.name, root_path if activity.owner %>
    <%= render_activity activity %>
  </div>
<% end %>